export const metadata = {
  title: "Privacy and Security",
  description:
    "Learn how PDFly handles uploaded files, temporary processing, and AI Summary data.",
};

export default function PrivacyLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
