export const metadata = {
  title: "Reorder PDF Pages",
  description: "Create a new PDF with pages in your chosen order.",
};

export default function ReorderPagesLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
