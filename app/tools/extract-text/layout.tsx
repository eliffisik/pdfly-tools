export const metadata = {
  title: "Extract PDF Text",
  description: "Extract readable text from a PDF into a downloadable TXT file.",
};

export default function ExtractTextLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
