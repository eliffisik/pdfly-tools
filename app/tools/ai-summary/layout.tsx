export const metadata = {
  title: "AI PDF Summary",
  description: "Extract readable PDF text and generate a concise AI summary.",
};

export default function AiSummaryLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
