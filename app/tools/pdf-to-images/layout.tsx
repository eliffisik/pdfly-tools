export const metadata = {
  title: "PDF to Images",
  description: "Convert PDF pages into downloadable PNG image files.",
};

export default function PdfToImagesLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
