export const metadata = {
  title: "Rotate PDF",
  description: "Rotate all pages or selected page ranges in a PDF file.",
};

export default function RotateLayout({ children }: { children: React.ReactNode }) {
  return children;
}
