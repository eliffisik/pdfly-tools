export const metadata = {
  title: "Ask PDF",
  description: "Ask questions and get answers from a PDF document.",
};

export default function AskPdfLayout({ children }: { children: React.ReactNode }) {
  return children;
}
