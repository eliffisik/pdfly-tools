export const metadata = {
  title: "Delete PDF Pages",
  description: "Remove selected pages from a PDF and download a cleaned file.",
};

export default function DeletePagesLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
