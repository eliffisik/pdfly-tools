export const metadata = {
  title: "Add Page Numbers",
  description: "Add page numbers to every page in a PDF file.",
};

export default function PageNumbersLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
