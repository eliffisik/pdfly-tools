export const metadata = {
  title: "Merge PDFs",
  description: "Combine multiple PDF files into one clean document with PDFly.",
};

export default function MergeLayout({ children }: { children: React.ReactNode }) {
  return children;
}
