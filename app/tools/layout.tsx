export const metadata = {
  title: "PDF Tools",
  description:
    "Browse PDFly tools for merging, splitting, compressing, converting, and organizing PDF files.",
};

export default function ToolsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
