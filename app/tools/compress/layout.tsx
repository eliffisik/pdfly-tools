export const metadata = {
  title: "Compress PDF",
  description: "Reduce PDF file size by rebuilding the document structure.",
};

export default function CompressLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
